export default class BasicUser{
    /**
     * 
     * @param {int} id 
     * @param {string} name 
     * @param {string} email 
     */
    constructor(id, name, email){
        this.id = id
        this.name = name
        this.email = email
    }
}