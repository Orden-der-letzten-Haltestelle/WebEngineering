<div id="cartPage-content">
    <!-- Load CartItems -->
    <div class="cartitems-container">
        <div class="cartitems-inner-container">

            <% cartItems.forEach((item)=> { %> <%- include("../../components/CartItem/CartItem", { item: item, url: url, token:
                    token }) %>
                    <% }) %>
        </div>
    </div>
    <div class="cartPage-button-container">onclick="handleDeleteCart('<%= token %>
        <button class="cartPage-button cart-delete" ')">Lösche Warenkorb</button>
        <a href="/checkout">
            <button class="cartPage-button cart-checkout">Zur Kasse</button>
        </a>
    </div>
</div>